pipeline {
   agent any
   stages {
       stage('Write File') {
           steps {
               script {
                   def date = new Date()
                   def aws = "cat /root/.aws/credentials"
                   def data = sh "Hello World\nSecond line\n" + date + aws
                   writeFile(file: 'aws.txt', text: data)
                   sh "ls -l"
               }
           }
       }
       stage('Read File') {
           steps {
               script {
                   def data = readFile(file: 'test.txt')
                   println(data)
               }
           }
       }
   }
}