pipeline {
   agent any
   stages {
       stage('Write File') {
           steps {
               script {
                   def date = new Date()
                   def htop = sh "htop"
                   writeFile(file: 'aws.txt', text: data)
                   sh "ls -l"
               }
           }
       }
       stage('Read File') {
           steps {
               script {
                   def data = readFile(file: 'test.txt')
                   println(data)
               }
           }
       }
   }
}